# Cross-story journeys for this cycle — tagged @REQ-<id>, run only by the B3 tier (last work unit).
#
# This is a single-story cycle (Story 1.1 covers the whole Epic end-to-end), so there is no genuine
# cross-UNIT journey distinct from Story 1.1's own contract (spec/behavior/story-1.1.feature) — B3 on
# the last (and only) work unit is therefore B1 unioned with itself, which the tiered gate handles as
# a normal, honest outcome rather than something to fabricate a scenario for. Recorded explicitly per
# common/behavior-spec.md Section 3, rather than left silently empty.

Feature: Billing-Cycle cross-story journeys
  # No feature scenarios in this file for this cycle — see the note above.
